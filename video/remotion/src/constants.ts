// ── Video Dimensions ────────────────────────────────────────────
export const WIDTH = 1080;
export const HEIGHT = 1920;
export const FPS = 30;

// ── Timing ──────────────────────────────────────────────────────
export const INTRO_DURATION_SECONDS = 0.5;
export const OUTRO_DURATION_SECONDS = 2.0;
export const TRANSITION_DURATION_FRAMES = 15;

// ── Captions (Korean-optimized) ─────────────────────────────────
export const CAPTION_SWITCH_MS = 900;
export const CAPTION_MAX_CHARS_PER_LINE = 10;
export const CAPTION_FONT_SIZE = 52;

// ── Slide Text ──────────────────────────────────────────────────
export const SLIDE_TEXT_FONT_SIZE = 42;
export const SLIDE_TEXT_LINE_HEIGHT = 1.7;
export const SLIDE_TEXT_STAGGER_FRAMES = 5;
export const SLIDE_TEXT_FADE_FRAMES = 10;

// ── Layout ──────────────────────────────────────────────────────
export const SLIDE_PADDING = 60;
export const SLIDE_NUMBER_FONT_SIZE = 28;
export const CAPTION_BOTTOM_OFFSET = 200;
export const SLIDE_MAX_TEXT_WIDTH = 960;
